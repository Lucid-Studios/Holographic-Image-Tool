name: Org Repository Standards

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  standards:
    uses: Lucid-Studios/.github/.github/workflows/repository-standards.yml@main
